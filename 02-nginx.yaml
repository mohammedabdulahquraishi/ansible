- name: installing nginx
  hosts: localhost
  become: yes
  tasks: 
  - name: install nginx package
    ansible.builtin.package:
    state: present
  - name: start nginx
    ansible.builtin.service:
    state: started  